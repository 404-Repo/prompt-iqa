# path to the checkpoint file
checkpoint_path: "checkpoints/best_model.pth.tar"

# path to the reference dataset
reference_dataset_path: "reference_dataset/"

# weights per image in the reference dataset
reference_weights: [0.0, 0.01, 0.2, 0.15, 0.6, 0.6, 0.95, 1.0]